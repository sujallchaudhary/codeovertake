# Backend API base (include the /api suffix)
VITE_API_URL=http://localhost:6754/api

# Clerk publishable key from the Clerk Dashboard > Configure > API keys.
# Social providers (Google, GitHub, ...) are toggled in the Clerk Dashboard;
# the sign-in UI picks them up automatically.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
